// Package tui will own the bubbletea Model/Update/View that drives
// chorus's interactive terminal UI (viewport + input box, replacing the
// line-based REPL in main.go). Scaffolding only for now — see the
// implementation plan for the phased build-out.
package tui
